{
  "name": "startuphub",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently -n client,server -c cyan,magenta \"npm --prefix client run dev\" \"npm --prefix server run dev\"",
    "db:generate": "npm --prefix database run generate",
    "db:migrate": "npm --prefix database run migrate",
    "seed": "npm --prefix database run seed",
    "build:server": "npm --prefix database ci && npm --prefix database exec prisma generate && npm --prefix server ci && npm --prefix server run build"
  }
}
